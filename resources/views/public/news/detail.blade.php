@extends('layouts.public')
@section('title', $article->title . ' | Makindye Ssabagabo Municipal Council')
@section('content')
<div class="news-page"><section class="news-hero"><div class="news-hero__overlay"></div><div class="news-hero__content"><span class="news-hero__crumb">Home / News &amp; Events / Article</span><h1>{{ $article->title }}</h1><p>◷ {{ optional($article->published_date)->format('F j, Y') }}</p></div></section><main class="news-detail-container"><a href="{{ route('news') }}" class="news-detail-back">← Back to News &amp; Events</a><article class="news-detail card-shell">@if($article->image)<img class="news-detail__image" src="{{ $media($article->image) }}" alt="{{ $article->title }}">@endif<div class="news-detail__body"><span class="news-card__date">◷ {{ optional($article->published_date)->format('F j, Y') }}</span><h2>{{ $article->title }}</h2><div class="news-detail__content">@foreach(preg_split('/\n\s*\n/', $article->body ?? 'This article does not have additional details yet.') as $paragraph)<p>{{ $paragraph }}</p>@endforeach</div>@if($article->external_url)<a href="{{ $article->external_url }}" target="_blank" rel="noreferrer" class="news-detail__external">View related source ↗</a>@endif</div></article></main></div>
@endsection
